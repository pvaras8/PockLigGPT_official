name: pockliggpt

channels:
  - conda-forge
  - pytorch

dependencies:
  - python=3.10

  # Core
  - numpy
  - pandas
  - pyarrow
  - pyyaml
  - scikit-learn
  - tqdm
  - requests
  - scipy

  # ML
  - pytorch
  - torchvision
  - torchaudio

  # Bio / química
  - rdkit
  - biopython

  # Notebook
  - jupyterlab

  # Pip
  - pip
  - pip:
      - omegaconf
      - selfies
      - transformers
      - huggingface_hub
      - meeko
      - vina
      - -e .
